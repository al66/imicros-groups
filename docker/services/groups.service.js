"use strict";

const { Groups } = require("imicros-groups");

module.exports = {
    name: "groups",
    mixins: [Groups],

    /**
     * Service settings
     */
    settings: {
        // to be done: db connection
    },

    /**
     * Service metadata
     */
    metadata: {},

};